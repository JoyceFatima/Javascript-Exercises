const a = (props) => {
  melhorPonto = 0;  
  piorPonto = 0;  
  for();
}

const Partidas = '7, 14, 13, 20, 17'
function Rounds() {
  if(partida > setPartida){
    `${setPartida}`
  } else (partida < setPartida)
}

console.log(Rounds)